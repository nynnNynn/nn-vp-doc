# 历史梗概 
- 解方程组：有没有解、有几个解、怎么快速求 
- Ax = b 赋予向量和空间语言 
- 矩阵作为类函数式的变换 
- 行列式判断这个映射是否“保体积 / 可逆 / 压扁空间 
- 特征那套研究映射中的稳定结构 
- 点积不知道在干啥 
## 第一章 
1. 线性就是 n个 cX = c 这种东西，这课未知数都能移到左边可以狭义理解，仅这种情况 
2. Rn就是有顺序的数包，一包数写在括号里，暂时也可以这么理解   
3. implicit 和explicit  我其实没搞清楚，可以理解为 某未知数 = 不含某未知数的表示法 就是explicit 显式，如果不是，多变量混在一个关系里就是implicit，但我不知道在微分方程里是不是也是这种判断 
4. row reduction没啥魔法，就是等式减等式加减左右不变，主要是rref唯一，这件事需要理解一下，换，减，乘除这事也没啥 
5. ref是每一行的第一个非零数越来越往右，且 pivot 下面全是 0，全0在最下面，上面啥样无所谓；rref是每个pivot变1，pivot所在列除了pivot都是0
6. 每个矩阵都能变成ref和rref，rref唯一
7. 参数形式一般来讲是把pivot所在列当主元，其他的都挪到右边，然后用非pivot列当变量，也是用这个当free variable，考试尽量这么写 
8. 解问题，就是增广列出现pivot，无解，出了00001，如果pivot = n，直接读解且唯一解，如果pivot < 列数，无穷多解
## 第二章
1. 2.1没啥魔力，linear combination只是展示这玩意为啥能变成 ax = b 那套东西 
2. consistent貌似意思是有无解，我也不知道为啥是这个词 
3. 考试理解右边是0就是homogenous，右边不是0就是nonhomogenous
4. 2.4前都没啥魔力，正着反着拼向量，右边不是0是b就是拼一个不带参数的向量，因为加乘法本来就支持么，当成拼向量就好了
5. subspace： 带原点，加封闭，乘封闭，就是取两个点，加起来乘起来带回去还符合规则 
6. 任何的向量span都是subspace
7. col space Col(A) 列向量直接一个一个写下来，基本上就是span出所有可能张成的b
8. nul space Nul(A) 就是所有满足 Ax = 0的非0解，基本就是对应能让他直接看出来线性相关的变量组成的这些东西，里解不出来什么意义，就写成使矩阵变0的空间吧，但主要是他跟线性相关有关，基本上nul里面几个向量，就是几个free variale
9. basis只要里面最小的线性无关张成待求平面，理论上算basis标准方法是给里面写成矩阵然后row reduction拿原矩阵的pivot列，实际上我不太知道，硬拼吧可能： 貌似没啥别的办法
10. 我现在也没搞明白咋回事，反正就知道rank + nuilty = n 
11. nontrivial是非0解，这词我不认识
12. m * n是m行n列，脑瓜子有点转不明白 就是 几个横行，几个等式和 n个未知数，后面的是列，是未知数 
## 第三章 
1. invert 算det(A) 然后整个除，对角线换，然后剩余俩加负号
2. 33 44 AI = IA-1 把左边消成 I 然后留后面的
3. 展 33 的det 从第一行往下 + - + 顺序 
4. 展44 det 消成倒三角然后乘对角线 
## 第四五章
5. 计算 det（44） 两行加减不变，如果某行乘了5，就除去5，如果换了一次，就乘一个-1
6. 找A 对角化(similarity ) 解det(A - lamdaI)= 0 找lamda和对应vector，把俩vector拼成C， 把lamda放对角拼成D
7. 判断diagonalize，若n个不同eigenvalue对应n列，即可被对角化，若有重复，看总维度等不等于eigenspace里的linear independent 向量个数 
8. stochastic 按列写 解长期状态 解 (A - I)v = 0 解v 和等于1 
## 第六章 
1. 点积有点tricky，一列就挨行乘加起来就完了 我不知道矩阵的，矩阵的那个好像就是乘法 两两即可
2. x.x等于 ||x|| ^2 
3. unit vector u = x / ||x||
4. 垂直就点积等于0，解方程就ok
5. w‘ 就是跟w里所有向量点积为0的东西
6. orthogonal 两两点积0， orthnormal 单个点积1 
7. gram schmidt 选定一个v1,然后后面的每一个序号的改造u
减去所有之前有过的 (uv/uu)u u是每一个，v是自己，比如求v3，都是自己 
8. least square AtAx = AtB 误差是 B - Ax