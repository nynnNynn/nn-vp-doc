# 300两数和
## code
```javascript
/**
 * @param {number[]} nums
 * @return {number}
 */
var lengthOfLIS = function(nums) {
    // 带着i的，是带着前面的最长
    let dp = new Array(nums.length);
    let max_length = 0;
    for(let i = 0; i < nums.length; i++){
        dp[i] = 
    }
};
```
## 总结