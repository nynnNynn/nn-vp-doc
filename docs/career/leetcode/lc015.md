# 015两数和
## code
```javascript
/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var threeSum = function(nums) {
    let result = [];
    for(let i = 0; i < nums.length; i++){

        // 走两数和呗
        let temp = {

        };
        // temp
        // 数据结构看一眼哈
        for(let j = i + 1; j < nums.length; j++){
            if(!temp[nums[j]])
            
        }
    }
    return result;
};
```
## 总结