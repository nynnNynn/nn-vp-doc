# 994 腐烂橘子
## code
```javascript
/**
 * @param {number[][]} grid
 * @return {number}
 */
var orangesRotting = function(grid) {
    let result = [];
    grid.forEach(
        (item, id) => {
            item.forEach(
                (alpha, idx) => {
                    if(alpha === 2){
                        result.push([id, idx]);
                    }
                }
            )
        }
    );
    // 第一个参数行 第二个参数列
    function testLegal(point){
        // 上下左右
        let result = [true,true,true,true];
        if(point[0] === 0 || grid[point[0] - 1][point[1]] === 2){
            result[0] = false;
        }
        if(point[0] === grid.length - 1 || grid[point[0] + 1][point[1]] === 2){
            result[1] = false;
        }
        if(point[1] === 0 || grid[point[0]][point[1] - 1] === 2){
            result[2] = false;
        }
        if(point[1] === grid.length - 1 || grid[point[0]][point[1] + 1] === 2){
            result[3] = false;
        }
        console.log(result);
    }
    // function letRot(points){
    //     if(points.)
    // }
    console.log(points.map((item) => testLegal(item)));

    // [ false, true, false, false ]
    // [ false, true, false, true ]
};
```
## 总结