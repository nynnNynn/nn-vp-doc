# 746使用最小花费爬楼梯
## code
```javascript
/**
 * @param {number[]} cost
 * @return {number}
 */
var minCostClimbingStairs = function(cost) {
    // 到n 有 n-1 爬或者 n-2 爬两种爬法  [10,15,20]  0 0 10爬俩或者15 爬一个  0 1 2 3 0 
    let dp = new Array(cost.length + 1).fill(0);
    for(let i = 2; i <= cost.length; i++){
        dp[i] = Math.min(dp[i - 1] + cost[i - 1], dp[i - 2] + cost[i - 2]);
    }
    return dp[dp.length - 1];

};
```
## 总结
1. 好像没啥特别特殊的，但是哪个循环上下界有点tricky其实
2. 可以顺手刷一下，比70爬楼梯强不少，算是done了吧