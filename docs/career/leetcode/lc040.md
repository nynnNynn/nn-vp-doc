# 040组合总和
## code
```javascript
/**
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
var combinationSum2 = function(candidates, target) {
    let result = [];
    function backtrace(candidate, sum, temp){
        if(sum === target){
            // 等于了，返回来，然后是pop，往下走是吧
            result.push(temp);
            return;
        }
        for( let i = 0; i < candidate.length; i++){
            backtrace()
            
        }

    }
    backtrace(candidates, []);
    return result;
    
};
```
## 总结