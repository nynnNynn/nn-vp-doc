# 217 存在重复元素
## code
```javascript
/**
 * @param {number[]} nums
 * @return {boolean}
 */
var containsDuplicate = function(nums) {
    let showed = new Set(nums);
    return showed.size !== nums.length;
};
```
## 总结
1. 好像没啥... 调原生api就行 