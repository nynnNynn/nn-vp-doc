# 022括号生成
## code
```javascript
/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) {
    let result = [];
    function backtrace(left, right, temp){
        if(left === right === 3){
            result.push(temp);
            return;
        }
        for(let i )
    }
    return result;
};
```
## 总结