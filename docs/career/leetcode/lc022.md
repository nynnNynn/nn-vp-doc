# 022括号生成
## code
```javascript
/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) {
    // abc 全排列么，这个是ab 然后a都得在b前面
    let result = [];
    function backrace(temp, used){
        if(temp.length === 2 * n){
            result.push(temp);
            return;
        }
        for(let i = 0; i < 2 * n; i+)

    }
    backtrace('',0);
    return result;
    
};
```
## 总结