# 039组合总和
## code
```javascript
/**
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
var combinationSum = function(candidates, target) {
    let result = [];
    function backtrace(items, temp){
        for(let i = 0; i < items.length; i++){
            
        }
    }
    return result;
};
```
## 总结