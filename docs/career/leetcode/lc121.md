# 121买卖股票最佳时机
## code
```javascript
/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
    let max = 0; 
    let min = Infinity;
    for(let i = 0; i < prices.length; i++){
        min = Math.min(prices[i], min);
        max = Math.max(max, prices[i] - min);
    }
    return max;
};
```
## 总结
1. 好像没啥，就是扫一遍，有历史最低价就记录，然后扫价差就行，好像还好
2. 但长时间不做这个就不知道，可能拿来练手吧，虽然今年这段可能最后一会找工作了