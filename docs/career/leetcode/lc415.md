# 415两数和
## code
```javascript
/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var addStrings = function(num1, num2) {
    // 双指针指末 如overflow 进位 1 flag = 1
 
    let ind1 = num1.length - 1;
    let ind2 = num2.length - 1;
    let flag = false;
    let result = '';
    while(ind1 >= 0 && ind2 >= 0){
        // 如无了
        // 52 + 189078 
        // 先进1，0 3 进 1
        // 看到false就往上加
        let temp = (Number(num1.charAt(ind1)) + Number(num2.charAt(ind2)));
        if(
            temp < 9
        ){
            if(flag){
                result = String(temp + 1) + result;
            }
            else {
                result = String(temp) + result;
            }
            flag = false;
            
        } else if(temp === 9) {
            if(flag){
                result = '0' + result;
                
            }
            else {
                result = '9' + result;
                flag = false;
            }
        } else {
            if(flag){
                result = String(temp - 9) + result;
            } else {
                result = String(temp - 10) + result;
                flag = true;
            }
        }
    }
    if(ind1 < 0){
        for(let )
    }
    return result;

    
};
```
## 总结